# packages for literature comparison
require(randomForest) # unsupervised random forest
require(clue) # CUBT
require(partitions) # CUBT
require(cubt) # CUBT
require(wskm) # two-level weighted kmeans
require(Rdimtools) # Laplacian Score